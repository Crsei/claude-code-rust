/**
 * Barrel for the skills management menu. Mirrors the upstream layout in
 * `ui/examples/upstream-patterns/src/components/skills/` (currently just
 * `SkillsMenu.tsx`).
 */
export { SkillsMenu, type SkillEntry, type SkillSource } from './SkillsMenu.js'
