# Better View 理想 UI 面板执行任务清单。
# 运行方式：
# .\scripts\run-standard-task-list-omx.ps1 -TasksFile .\docs\scripts\better-view-ui-panels-omx-tasks-2026-05-11.txt -OutputRoot .\target\codex-runs\better-view-ui-panels
#
# 范围：实现 docs/ui/better-view 中记录的五类理想面板族：
# settings-panels、selector-surfaces、wizard-flows、external-flows 和 approval-panels。
# 仅允许在有明确边界的只读映射、文件所有权不重叠的独立 surface 实现或 review 中使用 native subagent。当前 batch 的主 agent 负责集成和验证；commit 由 runner 负责。

[checkpoint] better-view-00 - 阅读 docs/ui/better-view/README.md、五个 Better View 面板文档、docs/COMMAND_UI_REFERENCE.md、docs/scripts/README.md，以及 crates/claude-code-rs/src/ui 下当前 UI 源码和测试所有权。记录 dirty worktree 路径、已有 CommandSurface/SelectionSurface/permission 覆盖、仍只存在于目标设计中的 surface，以及可行的定向测试。只允许 native subagent 做独立只读映射。返回带精确证据的 PASS/WARNING/BLOCKER，不修改代码。

[checkpoint] panel-shell-00 - 盘点 command_surface、selection_surface、form_navigation、keyboard_shortcut、tabs、diff preview、permission request view 和 snapshot 测试中可复用的渲染 primitive。定义支持 sections、详情面板行、action metadata、source/current/risk 标记和固定 footer 所需的最小共享 Better View 面板模型。不要写实现代码。
[build] panel-shell-01 - 在 crates/claude-code-rs/src/ui/components 下使用现有 ratatui/string 渲染模式新增共享 Better View 面板渲染 primitive。只拥有新增 primitive 模块、最小导出和聚焦的 primitive snapshot 测试。覆盖只读行、可执行行、fill-prompt 行、source/current/risk 标记、选中标记和固定 footer。
[review] panel-shell-02 - 验证共享面板 primitive 没有过度抽象，没有不必要地重复已有 tabs/selection/diff helper，在可行处保持 snapshot 文本 ASCII 安全，并能表示全部五类 Better View 面板族。只做 review 修复，并运行 primitive snapshot 测试和 git diff --check。

[checkpoint] settings-panels-00 - 盘点 docs/ui/better-view/settings-panels.md 中当前和目标设置类 surface：Config、Sandbox、Hooks、MCP、Login、Permissions、Memory、Plugin 和 Runtime Preferences。把每个 section row 映射到已有 command handler、CommandSurfaceOutcome 变体、AppState 数据、risk/source/current 标签和缺失的后端数据。不要写代码。
[build] settings-panels-01 - 将 ConfigSurface 迁移或扩展到共享 Better View shell，覆盖 Status、Model、Theme、Usage、Output、Language、Thinking、Safety 和 Config sections，同时保留无参数 /config 行为以及现有 /config set/show/sources/schema 命令。运行 cargo test -p claude-code-rs config_cmd command_surface -- --nocapture。
[build] settings-panels-02 - 将 SandboxSurface 迁移或扩展到共享 Better View shell，覆盖 Config、Dependencies、Doctor、Mode 和 Network sections。保持 dependency rows 只读，显式展示不可用依赖，并为 disable sandbox、full mode 和 network-on 动作标记 risk metadata。运行 cargo test -p claude-code-rs sandbox command_surface -- --nocapture。
[review] settings-panels-03 - Review Config 和 Sandbox 变更，检查命令语义一致性、source/current/risk 可见性、picker filter 行为、无效状态不被隐藏成默认值，以及带参数 slash command 无行为漂移。只做 review 修复，并重跑失败的定向测试。
[build] settings-panels-04 - 将 Hooks、MCP、Login 和 Memory surfaces 迁移或扩展到共享 Better View shell，同时保留现有 scope/action 快捷键：/hooks list/open、/mcp status/edit/reconnect/remove/add、/login status/1-5、/memory edit/show/path/open。运行 cargo test -p claude-code-rs hooks mcp memory command_surface -- --nocapture。
[build] settings-panels-05 - 仅在现有 command handler 和 state source 能支撑的范围内新增或接线 Permissions、Plugin 和 Runtime Preferences 目标 surface。显式保留不支持的 rows，不虚构 plugin install 行为，保留 disable/uninstall/reset 的 direct-execute 语义，并展示 runtime-only 与持久化设置状态的区别。运行 cargo test -p claude-code-rs permissions command_surface -- --nocapture，以及存在的 plugin/runtime 聚焦命令测试。
[review] settings-panels-06 - Review 全部 settings-family surfaces，检查统一 shell 一致性、稳定左侧导航、单一 footer、正确的 Enter 与 fill-prompt 行为、direct-execute 标签，以及没有新增依赖。只做 review 修复，并运行 cargo test -p claude-code-rs command_surface -- --nocapture。
[final] settings-panels-07 - 运行 settings-family gate：git diff --check，cargo test -p claude-code-rs command_surface config_cmd sandbox permissions hooks mcp memory -- --nocapture；只有行为发生变化时才更新实现追踪文档。返回 PASS/WARNING/BLOCKER，并列出仍不支持的 settings rows。

[checkpoint] selector-surfaces-00 - 盘点 docs/ui/better-view/selector-surfaces.md 中当前和目标 selector-family surfaces：command palette、edit target picker、agents、diff、skills、tasks、team、LSP recommendation 和 history search。把每个 surface 映射到已有文件、outcomes、插入与执行语义、detail views、direct-execute 快捷键和 snapshot 覆盖。不要写代码。
[build] selector-surfaces-01 - 将 command palette 和 edit target picker 升级为 Better View list/detail 形态，同时保留 typed filtering、Enter 插入命令文本而不是执行带参数命令，以及 Ctrl+E 只插入 target argument。运行 cargo test -p claude-code-rs command_palette -- --nocapture。
[build] selector-surfaces-02 - 将 Agents、Skills 和 History Search selector surfaces 升级到 Better View shell，包含 source/filter 状态、detail preview、可用时展示 disabled reason，以及 history 的 fill-prompt 语义。除非已有安全的 save/cancel 路径，否则保持 create-agent wizard 隐藏。运行 cargo test -p claude-code-rs agents skills history_search command_surface -- --nocapture。
[review] selector-surfaces-03 - Review command palette、edit target、agents、skills 和 history 变更，检查 insert-versus-execute 正确性、filter 焦点冲突、create-agent 隐藏行为和 snapshot 稳定性。只做 review 修复，并重跑聚焦测试。
[build] selector-surfaces-04 - 将 Diff 和 Tasks selector surfaces 升级为 Better View list/detail 行为。确保 Enter 在文档要求处进入 detail，b 从 detail 返回，task stop/delete 命令明确标记为 direct-execute，team-backed task stop 映射到 /team kill <name>。运行 cargo test -p claude-code-rs diff tasks command_surface -- --nocapture。
[build] selector-surfaces-05 - 将 Team 和 LSP recommendation surfaces 升级为 Better View list/detail 行为。inactive team 只显示 create/list actions，保留 /team send/kill/spawn 路由，并保持 LSP yes 行为为 fill prompt /plugin install <name>，不声称未支持的 plugin install 会被执行。运行 cargo test -p claude-code-rs teams lsp_recommendation command_surface -- --nocapture。
[review] selector-surfaces-06 - Review 全部 selector-family surfaces，检查快捷键一致性、detail view 可逆性、direct-execute 标签、filter persistence、source tab reset，以及没有命令行为漂移。只做 review 修复，并运行 cargo test -p claude-code-rs command_palette selection_surface command_surface -- --nocapture。
[final] selector-surfaces-07 - 运行 selector-family gate：git diff --check，cargo test -p claude-code-rs command_palette selection_surface agents skills diff tasks teams lsp_recommendation history_search command_surface -- --nocapture；将缺失的后端数据记录为 follow-up，不要用假 rows 掩盖。

[checkpoint] wizard-flows-00 - 盘点 docs/ui/better-view/wizard-flows.md 中当前和目标 wizard-family flows：Login OAuth、MCP Auth、Plan workflow、Permissions Plan Mode 和 Create Agent wizard target。将每个 command string 映射到现有 handlers/components、副作用、审批语义、external-editor handoff 和不可用的 target-only 行为。不要写代码。
[build] wizard-flows-01 - 新增复用共享 Better View shell 的 wizard panel view model/render path，支持 step list、selected step detail、command preview、safety/verification notes 和 fixed footer。打开 wizard panel 不得启动 OAuth、编辑文件或发起网络请求。运行聚焦 UI primitive 测试。
[build] wizard-flows-02 - 将 Login OAuth 和 MCP Auth wizard panels 接到现有 /login 2|3|4 和 /mcp auth start/complete command preview。把 external URL output 作为独立 flow，保持 completion commands 可见，并且不暗示内嵌浏览器支持。运行 cargo test -p claude-code-rs login mcp command_surface -- --nocapture。
[review] wizard-flows-03 - Review OAuth 和 MCP auth wizard 变更，检查 command-handler 语义一致性、打开时无副作用、state-token/completion instructions 可见，以及没有重复 external-flow renderer。只做 review 修复，并重跑聚焦测试。
[build] wizard-flows-04 - 将 Plan workflow 和 Permissions Plan Mode panels 接到现有 /plan enter/open/edit/approve/reject 和 /permissions mode plan 行为。保留 plan approval gates，展示 no-file-edits-while-planning 验证规则，并把 /plan open/edit 视为 external editor handoff。运行 cargo test -p claude-code-rs permissions plan command_surface -- --nocapture 或最接近的聚焦测试。
[build] wizard-flows-05 - 将 Create Agent wizard target 放在现有安全 agent 创建路径之后做门控。如果 save/cancel 接线无法安全复用已有 AgentSettingsCommand user/project settings 路径，则保持 wizard 隐藏，并记录准确的 target-only gap。运行 cargo test -p claude-code-rs agents command_surface -- --nocapture。
[final] wizard-flows-06 - 运行 wizard-family gate：git diff --check，cargo test -p claude-code-rs agents login mcp permissions command_surface -- --nocapture；更新 Better View 或 tracker docs，记录已实现 wizard panels 和 target-only gaps。

[checkpoint] external-flows-00 - 盘点 docs/ui/better-view/external-flows.md 中当前和目标 external-flow panels：OAuth URL output、external editor open、Chrome/IDE bridge 和 /session no-QR output。将它们映射到真实命令、平台 helper、fallback paths、state sources 和 text-only 兼容说明。不要写代码。
[build] external-flows-01 - 为 /login 2|3|4 和 /mcp auth start 输出实现或升级 external authorization panel。展示 provider、status、URL、browser completion step、paste/complete command 和 notes，但面板 renderer 不打开也不嵌入浏览器。运行聚焦 login/mcp output 测试。
[build] external-flows-02 - 为 /hooks open <layer>、/keybindings open、/plan open 和 /plan edit flows 实现或升级 external editor panel。展示 target path、相关时的文件创建状态、editor command，以及未配置 editor 时 fallback-to-path 行为。运行聚焦 hooks/keybindings/plan command 测试。
[review] external-flows-03 - Review authorization 和 editor external-flow panels，检查副作用文案真实、.cc-rust 路径隔离准确、command/path 文本可复制、没有静默 editor fallback，以及没有浏览器/IDE 嵌入声明。只做 review 修复，并重跑聚焦测试。
[build] external-flows-04 - 实现或升级 Chrome/IDE bridge 和 /session panels。展示 help/reconnect command previews、可用时展示 selected IDE、bridge status，以及当前 Rust /session 行为的 remote_qr=unsupported。除非存在单独明确的行为变更，否则不要增加 QR 渲染。运行聚焦 chrome/ide/session command 测试。
[final] external-flows-05 - 运行 external-flow gate：git diff --check，cargo test -p claude-code-rs hooks keybindings ide session login mcp command_surface -- --nocapture 或最接近的现有聚焦测试；在 tracker docs 中记录不支持的 QR/bridge 场景。

[checkpoint] approval-panels-00 - 盘点 docs/ui/better-view/approval-panels.md 中当前和目标 approval-family panels：shell/PowerShell tool permission、file edit/write/sed permission、AskUserQuestion、MCP project server approval、workspace trust 和 direct-execution status。将每项映射到现有 permission renderers、decision callbacks、scope persistence、Esc 行为和 snapshot 覆盖。不要写代码。
[build] approval-panels-01 - 升级 shell 和 PowerShell tool approvals 的共享 permission request 渲染，展示 tool kind、command preview、requester/context、risk hint、decisions、persistence scope 和 fixed footer。Esc 必须 deny。运行 cargo test -p claude-code-rs permissions bash_permission_request power_shell_permission_request -- --nocapture 或最接近的聚焦 filters。
[build] approval-panels-02 - 升级 file write/edit/sed permission panels，在 decisions 前展示一等 diff/patch preview，包含 operation、target path、summary、allow/deny choices，以及显式 always-allow path/request scope。复用已有 diff helpers。运行 cargo test -p claude-code-rs file_edit_permission_request file_write_permission_request sed_edit_permission_request diff -- --nocapture。
[review] approval-panels-03 - Review tool 和 file approval panels，检查 risk/scope labels 准确、没有重复 diff rendering logic、Esc deny 语义一致，以及 previewed diff 与 approved action 不错位。只做 review 修复，并重跑聚焦测试。
[build] approval-panels-04 - 升级 AskUserQuestion approval panels，展示 question progress、可见 multiple-choice state、Back/Next/Submit 区别，以及不丢失已选答案的 cancel 行为。运行 cargo test -p claude-code-rs ask_user_question_permission_request permissions -- --nocapture。
[build] approval-panels-05 - 升级 MCP single-server 和 multi-server approval panels，展示 risk statement、source、可选择的 server decisions、派生 approve/reject commands、适用时的 approve-all future scope，以及 Esc reject 行为。运行 cargo test -p claude-code-rs mcp_server_approval_dialog mcp_server_multiselect_dialog mcp -- --nocapture。
[build] approval-panels-06 - 升级 Workspace Trust 和 Direct Execute status panels。Workspace Trust 必须展示精确 path 和不同的 Trust/Exit/Reject 语义；Direct Execute 必须记录 /tasks delete/stop、/team kill/delete/leave、/logout、/clear、/mcp remove、/plugin disable/uninstall 和 /permissions reset 当前无确认的行为，不绘制 confirm modal。运行聚焦 app/workspace_trust/command 测试。
[review] approval-panels-07 - 从安全角度 review approval-family 变更，检查 always-allow scope 过宽、direct-execute 语义模糊、workspace path 截断、MCP approve-all persistence 和 approval callback 幂等性。只修 blocker 或 test failure，并重跑聚焦测试。
[final] approval-panels-08 - 运行 approval-family gate：git diff --check，cargo test -p claude-code-rs permissions mcp app command_surface -- --nocapture；仅对当前仍存在的用户可感知 approval 问题更新 docs/KNOWN_ISSUES.md。

[review] better-view-90 - 跨 lane review 完整 Better View 实现。验证五类 panel families 共用一致的 shell，无参数 slash commands 仍打开 surfaces，而带参数形式仍进入 command handlers，insert 与 execute 语义保持不变，source/current/risk 标签可见，没有新增依赖，file-size guards 可接受，并且 target-only gaps 已记录。
[final] better-view-99 - 运行 Better View UI panel 计划的最终验证：git diff --check，cargo fmt --all --check，cargo test -p claude-code-rs command_surface command_palette selection_surface permissions mcp agents tasks teams skills history_search -- --nocapture，并在 snapshot tooling 可用时运行 scripts/export-ui-snapshots.ps1 -CheckOnly。仅在需要时更新 docs/RATATUI_UI_PARITY.md、docs/WORK_STATUS.md、docs/IMPLEMENTATION_GAPS.md、docs/KNOWN_ISSUES.md 和 docs/archive/better-view-ui-panels-execution-report-2026-05-11.md，汇总已实现效果、缺陷、follow-ups 和剩余风险。
