# One task per line for scripts/run-ratatui-ui-parity-omx.ps1. Tags [checkpoint], [review], and [final] force a single-task batch.
[checkpoint] Baseline/status gate: read docs/plan/ratatui-ui-parity-omx-execution-plan-2026-05-08.md, docs/plan/ratatui-ui-parity-untracked-gap-plan-2026-05-08.md, docs/COMMAND_UI_REFERENCE.md, scripts/codex-task-sequence.md, and docs/scripts/README.md. Do not write files. Verify the intended task order, current dirty worktree, available tests, and whether omx exec can be used from this workspace. Return PASS/BLOCKER/WARNING plus exact next command if blocked.
[build] Shared primitives A: implement the smallest shared tabs/status-icon/keyboard-shortcut hint primitives under crates/claude-code-rs/src/ui/components without broad migration. Use existing render string patterns and snapshots. Own only new primitive modules, ui/components/mod.rs exports if needed, and the smallest consumer/test required to prove integration. Run cargo test -p claude-code-rs command_surface selection_surface -- --nocapture or the nearest targeted tests.
[build] Shared primitives B: extend selection/fuzzy list behavior only as needed for preview/actions/disabled reason/multi-line rows. Keep existing /config, /mcp, /skills, /tasks snapshots stable unless behavior intentionally improves. Do not migrate /agents in this task. Run cargo test -p claude-code-rs selection_surface command_palette command_surface -- --nocapture.
[review] Review A shared primitive gate: inspect the diff and last-message outputs from the shared primitive tasks. Do not add new features. Identify BLOCKER/WARNING/FOLLOW-UP items, especially over-generalized abstractions, file-size violations, broad signature churn, or duplicated safety/rendering logic. If fixes are required, make only review fixes and run the targeted tests named by the failed tasks.
[build] Settings dashboard: expand /config toward the plan's Settings dashboard with usage/output/language/thinking/safety tabs only where existing command/config state already supports it. Prefer form rows and existing /config command paths. Do not implement sandbox or permissions here. Run cargo test -p claude-code-rs config_cmd command_surface -- --nocapture.
[build] Sandbox surface: expand /sandbox with Config/Dependencies/Overrides/Doctor style status where existing sandbox command/runtime data supports it. Make unsupported or unavailable dependencies explicit instead of silently defaulting. Do not edit permissions or config dashboard files unless a shared helper from the prior task requires it. Run cargo test -p claude-code-rs sandbox command_surface -- --nocapture.
[build] Permissions safety UI: add the minimal /permissions command surface and wire AutoModeOptIn/BypassPermissions-style confirmation states using existing permission rule renderers and command APIs. Confirm WebFetch requests use the dedicated renderer or record the exact routing gap. Keep mutation paths explicit and .cc-rust isolated. Run cargo test -p claude-code-rs permissions command_surface -- --nocapture.
[review] Review B settings and safety gate: review /config, /sandbox, and /permissions changes. Check that errors are visible, present-invalid state does not become empty/default, and no redundant safety layers were added. Make only review fixes. Run targeted tests for changed files and git diff --check.
[build] Message interactions: implement selected-message state, concise timestamps when real metadata exists, copy/detail actions, path/image reference display, compact summary, interrupted-by-user, and clearer fallback/rejected/error renderers. Do not change prompt/composer logic in this task. Run cargo test -p claude-code-rs messages -- --nocapture.
[build] Prompt/composer UX: add mode indicator, unified shortcut/command hints, placeholder resolver, large paste display, and UI-only input truncation preview. Do not alter message renderer behavior from the previous task except to connect shared hints. Run cargo test -p claude-code-rs prompt_input history_search command_palette -- --nocapture.
[build] Agents surface: upgrade /agents from selector-only to list/detail mode and wire create-agent wizard entry only if save/cancel can use existing agent settings paths safely. Keep /agents show <name> text behavior. Decide whether /agent alias is needed and document the decision. Run cargo test -p claude-code-rs agents command_surface -- --nocapture.
[build] Team/task status surfaces: add or refine agent tree/coordinator status/team member card/team summary/shell progress detail using existing teams/tasks models. Keep file ownership inside ui/teams, ui/tasks, app/agent_navigation, and related command status files. Run cargo test -p claude-code-rs teams tasks command_surface -- --nocapture.
[review] Review C cross-surface refactor gate: inspect all message, prompt, agents, teams, and tasks changes. Block broad refactors, repeated renderer helpers, files over the configured size threshold, or changes that mix unrelated domains. Make only review fixes and rerun the smallest failing test set.
[build] Search/session/help: implement persistent-history reader integration for Ctrl+R if backend data exists, plus quick open/global search/log selector/session preview/export/exit-flow/Help V2/diagnostics/keybinding warnings in small surfaces. Reuse existing session/export/help/doctor/keybinding commands. Run cargo test -p claude-code-rs resume session_export command_palette -- --nocapture.
[build] IDE/LSP/Chrome/remote decision: add IDE picker/status, LSP server card, and Chrome onboarding/status surfaces where current commands expose real data. For remote/teleport items, either wire to existing remote-control/channel data or mark crop/deferred in docs with rationale. Run cargo test -p claude-code-rs ide lsp_recommendation channels -- --nocapture.
[final] Final verification and documentation closeout: read every batch summary and task last-message file under target/codex-runs/ratatui-ui-parity-omx. Run git diff --check and the broad targeted UI test set that is feasible. Update docs/RATATUI_UI_PARITY.md, docs/WORK_STATUS.md, docs/IMPLEMENTATION_GAPS.md or docs/KNOWN_ISSUES.md as needed. Save implementation effects, defects, and follow-up tracking to docs/archive/ratatui-ui-parity-omx-execution-report-2026-05-08.md. Return PASS/BLOCKER plus remaining risks.
