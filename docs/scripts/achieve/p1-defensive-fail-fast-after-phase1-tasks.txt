# One task per line for scripts/codex-task-sequence.ps1. Phase 1 is already complete; start from Phase 2.
Phase 2A - Hook criticality schema and regression tests: read docs/plan/p1-defensive-fail-fast-execution-plan-2026-05-07.md Phase 2 and docs/TECH_DEBT.md P1 fail-fast section; do not revisit Phase 1 unless a regression blocks this task; do not revert unrelated dirty worktree changes. Locate hook config types and parse points, add the smallest existing-pattern critical/optional representation needed for hooks, keep existing hooks optional by default unless current schema already implies blocking behavior, and add tests that prove default optional compatibility plus critical config parsing. Own only hook config/type/test files needed for this schema step. Run cargo test -p claude-code-rs tools::hooks -- --nocapture and cargo check -p claude-code-rs --message-format short.
Phase 2B - Critical pre-tool hooks fail closed: read Phase 2 plan and preserve Phase 2A schema. Update crates/claude-code-rs/src/tools/hooks/pre_tool.rs and crates/claude-code-rs/src/engine/lifecycle/deps.rs so optional pre-tool hook errors still warn/continue, but critical pre-tool hook errors block the current tool step with a visible error. Add or update tests for both optional-continue and critical-block behavior. Do not edit post-hook, plugin, MCP, auth, mailbox, or TaskStore files. Run cargo test -p claude-code-rs tools::hooks -- --nocapture, cargo test -p claude-code-rs engine::lifecycle -- --nocapture, and cargo check -p claude-code-rs --message-format short.
Phase 2C - Critical post/failure/stop hooks and hook IO diagnostics: read Phase 2 plan and preserve Phase 2A/2B behavior. Update crates/claude-code-rs/src/tools/hooks/post_tool.rs and crates/claude-code-rs/src/tools/hooks/execution.rs so critical post/failure/stop hook failures are visible according to the same policy and stdin/stdout/stderr/kill IO failures are recorded in hook result data or tracing. Optional hooks must remain best-effort. Add targeted tests. Do not edit MCP/plugin/auth/task/team files. Run cargo test -p claude-code-rs tools::hooks -- --nocapture, cargo test -p claude-code-rs engine::lifecycle -- --nocapture, and cargo check -p claude-code-rs --message-format short.
Phase 3 - Remove duplicate tool orchestration path: read Phase 3 plan. Confirm with rg that production code does not depend on crates/claude-code-rs/src/tools/orchestration.rs; remove the tools/mod.rs export and delete or quarantine orchestration.rs as test-only, keeping QueryDeps::execute_tool and crates/claude-code-rs/src/tools/execution/mod.rs as the only production execution path. Remove related unused/dead allow attributes only when compile proves they are no longer needed. Do not change hook policy semantics from Phase 2. Run cargo test -p claude-code-rs tools::execution -- --nocapture, cargo test -p claude-code-rs query::loop_helpers -- --nocapture, and cargo check -p claude-code-rs --message-format short.
Phase 4A - MCP discovery fail-fast diagnostics: read Phase 4 plan. In crates/cc-mcp/src/discovery.rs, distinguish missing config files from existing-but-invalid user/project settings; missing stays empty, existing read/parse errors become diagnostics/errors, and a malformed single server preserves name/scope/error instead of disappearing. Update claude-code-rs MCP discovery call sites that currently unwrap_or_default so diagnostics are visible or propagated. Do not edit plugin/auth/startup/config mutation files. Run cargo test -p cc-mcp discovery -- --nocapture and cargo check -p claude-code-rs --message-format short.
Phase 4B - Plugin loader diagnostics: read Phase 4 plan. In crates/claude-code-rs/src/plugins/loader.rs, plugins/mod.rs, and plugins/refresh.rs, stop treating corrupt installed_plugins.json, unreadable cache traversal, or malformed plugin manifests as simply no plugins. Return structured diagnostics and surface plugin metadata invalid state in startup/status paths while keeping truly absent plugin metadata as empty. Add tests for corrupt installed_plugins.json and malformed manifest handling. Do not edit MCP/auth/startup/config mutation files. Run cargo test -p claude-code-rs plugins -- --nocapture and cargo check -p claude-code-rs --message-format short.
Phase 4C - Config mutation, startup env, and permission parsing fail fast: read Phase 4 plan. Update commands/memory.rs and commands/model_add.rs so existing unreadable/invalid config or .env files abort mutation without rewrite; update startup/mod.rs so existing .env load errors surface as diagnostics; update cc-types/src/permissions.rs so unknown configured permission mode does not parse as Default while absent mode still defaults. Add targeted tests. Do not edit MCP/plugin/auth files. Run cargo test -p claude-code-rs commands::memory -- --nocapture, cargo test -p claude-code-rs commands::model_add -- --nocapture, cargo test -p cc-types permissions -- --nocapture, and cargo check -p claude-code-rs --message-format short.
Phase 4D - Auth errors are not plain no-auth: read Phase 4 plan. In crates/cc-auth/src/lib.rs and directly related auth tests, distinguish absent credentials from present invalid/unreadable credentials, invalid present API keys, keychain/token load failures, and token refresh runtime/infrastructure failures. Do not clear credentials except on confirmed revoked/invalid_grant style outcomes. Add tests proving infrastructure failure is diagnostic and does not become Ok(None). Do not edit MCP/plugin/config/startup files. Run cargo test -p cc-auth --lib and cargo check -p claude-code-rs --message-format short only if claude-code-rs auth call sites are touched.
Phase 5A - Concurrent tool result cardinality: read Phase 5 plan. In crates/claude-code-rs/src/query/loop_helpers.rs, capture tool_use_id and tool_name before spawning concurrent tool tasks, synthesize a failed ToolExecResult for any JoinError/panic, and remove unknown id/name fallbacks where original context is available. Add a regression test where a spawned tool panic still emits a failed result with the original tool_use_id. Do not edit API streaming or output style files. Run cargo test -p claude-code-rs query::loop_helpers -- --nocapture and cargo check -p claude-code-rs --message-format short.
Phase 5B - Streaming required fields and output style fallback diagnostics: read Phase 5 plan. In crates/claude-code-rs/src/api/streaming.rs, classify required versus optional SSE fields and return parse errors for missing/malformed required usage/index/content_block/delta data instead of unwrap_or_default/unwrap_or(0). In crates/claude-code-rs/src/engine/output_style.rs, make unknown configured style or missing custom style produce a visible fallback diagnostic while preserving explicit Default behavior. Add targeted tests. Do not edit query loop helper files. Run cargo test -p claude-code-rs api::streaming -- --nocapture, cargo test -p claude-code-rs engine::output_style -- --nocapture, and cargo check -p claude-code-rs --message-format short.
Phase 6 - Team coordination and worktree isolation failures are visible: read Phase 6 plan. In crates/claude-code-rs/src/teams/runner.rs, promote coordination-affecting mailbox processing errors from warn-only to task-visible failure or structured diagnostic; gate ShutdownRequest auto-approval behind explicit non-interactive policy or documented mode; in crates/claude-code-rs/src/engine/agent/supervisor.rs, make worktree isolation fallback visible and opt-in when isolation is required. Do not edit mailbox parser unless a minimal integration adjustment is required by Phase 1 strictness. Run cargo test -p claude-code-rs teams::runner -- --nocapture, cargo test -p claude-code-rs engine::agent -- --nocapture, and cargo check -p claude-code-rs --message-format short.
Phase 7 - Documentation closure and touched-file allow cleanup: read docs/plan/p1-defensive-fail-fast-execution-plan-2026-05-07.md Phase 7, docs/TECH_DEBT.md P1 section, and target/codex-runs/p1-defensive-after-phase1 task last-message files if present. Update docs/TECH_DEBT.md with completed Phase 2-6 status, changed files, verification run, and remaining risks; move completed entries to docs/archive/TECH_DEBT.md only when all related tests passed; remove broad allow(dead_code/unused/unused_imports) only from files touched by completed phases and only when cargo check stays green. Run git diff --check, cargo check -p claude-code-rs --message-format short, and the targeted tests from completed phases that are needed to support the doc status.
