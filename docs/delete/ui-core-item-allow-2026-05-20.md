# UI core item allow cleanup - 2026-05-20

## Removed allow attributes

- `crates/claude-code-rs/src/ui/prompt_input.rs`: removed item-level `#[allow(dead_code)]` from test-only prompt inspection/render helper methods. Reason: the helpers remain available under `#[cfg(test)]`; the allow was redundant for production. Restore by re-adding the specific item attribute if a test-target dead-code lint is intentionally enforced.
- `crates/claude-code-rs/src/ui/command_palette/mod.rs`: removed `#[allow(dead_code)]` from test-only command suggestion helpers and `#[allow(unused_imports)]` from the public `CommandGroup` re-export. Reason: command suggestion helpers remain in place; the re-export needed a separate deletion decision after `cargo check` proved it was unused. Restore the allow attributes by re-adding them to the specific items if lint policy changes.
- `crates/claude-code-rs/src/ui/input/path_completion.rs`: removed `#[allow(dead_code)]` from the test-only file-inclusion setter. Reason: test configuration surface remains under `#[cfg(test)]`; suppression was redundant. Restore by re-adding the item attribute if needed.
- `crates/claude-code-rs/src/ui/input/slack_channel_completion.rs`: removed `#[allow(dead_code)]` from the test-only known-channel setter. Reason: test configuration surface remains under `#[cfg(test)]`; suppression was redundant. Restore by re-adding the item attribute if needed.
- `crates/claude-code-rs/src/ui/input/shell_history_completion.rs`: removed `#[allow(dead_code)]` from the test-only shell-history invalidation hook. Reason: hook remains under `#[cfg(test)]`; suppression was redundant. Restore by re-adding the item attribute if needed.
- `crates/claude-code-rs/src/ui/theme/mod.rs`: removed item-level `#[allow(dead_code)]` from test-only theme labels, auto-refresh helpers, theme list access, and save helper. Reason: all objects are retained; no production deletion or behavioral narrowing. Restore by re-adding the specific item attribute if stricter test linting requires it.
- `crates/claude-code-rs/src/ui/rendering/shimmer.rs`: removed `#[allow(dead_code)]` from `ShimmerAnimation::with_hue`. Reason: the module is test-gated at the UI facade and the constructor is a retained compatibility surface. Restore by re-adding the item attribute if this module is compiled in a target with dead-code warnings promoted.
- `crates/claude-code-rs/src/ui/rendering/virtual_scroll.rs`: removed `#[allow(dead_code)]` from the test-only logical visible-range helper. Reason: width-aware production code uses `visual_range`; the retained helper is test-only diagnostics. Restore by re-adding the item attribute if needed.
- `crates/claude-code-rs/src/ui/rendering/syntax_highlight.rs`: removed item-level `#[allow(dead_code)]` from syntect token normalization helper and test-only supported-language list. Reason: syntect-enabled production uses the token helper; the language list is retained for tests. Restore by re-adding the specific item attribute if lint policy changes.
- `crates/claude-code-rs/src/ui/rendering/tool_activity.rs`: removed `#[allow(dead_code)]` from the test-only compact text renderer. Reason: styled production rendering is already wired; the plain renderer remains a test/snapshot helper. Restore by re-adding the item attribute if needed.
- `crates/claude-code-rs/src/ui/diff.rs`: removed `#[allow(dead_code)]` from the test-only structured hunk accessor. Reason: production diff surfaces use raw hunks and structured parsers remain test-gated. Restore by re-adding the item attribute if needed.
- `crates/claude-code-rs/src/ui/app/input.rs`: removed `#[allow(dead_code)]` from the test-only completion selection helper. Reason: production currently uses previous-selection on BackTab; helper remains for test coverage. Restore by re-adding the item attribute if needed.
- `crates/claude-code-rs/src/ui/app/status.rs`: removed `#[allow(dead_code)]` from the test-only status-line runner accessor. Reason: production state injection remains wired via `set_status_line_runner`; accessor remains test-only. Restore by re-adding the item attribute if needed.
- `crates/claude-code-rs/src/ui/app/app_event.rs`: removed item-level `#[allow(dead_code)]` from test-only app event variants. Reason: production event routing uses `Backend`; local notice, notification, tick, and shutdown variants remain test infrastructure. Restore by re-adding the specific variant attribute if needed.
- `crates/claude-code-rs/src/ui/app.rs`: removed item-level `#[allow(dead_code)]` from test-only app state accessors/mutators. Reason: public production behavior is unchanged; helpers remain `#[cfg(test)]`. Restore by re-adding the specific item attribute if needed.
- `crates/claude-code-rs/src/ui/helpers/skills_helpers.rs`: removed item-level `#[allow(dead_code)]` from test-only skill truncation and matching helpers. Reason: production skill display helpers stay wired; test-only matching helpers remain available. Restore by re-adding the specific item attribute if needed.
- `crates/claude-code-rs/src/ui/platform/terminal_env.rs`: removed `#[allow(dead_code)]` from the test-only mouse-runtime support constant. Reason: runtime mouse env handling is wired through `TerminalEnvConfig`; the constant remains test-only. Restore by re-adding the item attribute if needed.

## Deleted objects

- `crates/claude-code-rs/src/ui/command_palette/mod.rs`: deleted `pub use filter::CommandGroup;` and its comment. Reason: after removing the `unused_imports` allow, `cargo check -p claude-code-rs` reported the re-export as unused; `command_palette/render.rs` imports `CommandGroup` directly from `filter`, so the re-export is duplicate surface rather than production wiring. Restore by re-adding `pub use filter::CommandGroup;` if an external command-palette consumer is introduced.

No functions, types, constants, enum variants, or production branches were deleted in this pass.
