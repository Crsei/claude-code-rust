[package]
name = "cc-ipc-adapters"
version = "0.1.0"
edition = "2021"
description = "Pure adapters between cc-rust SDK/query events and IPC protocol payloads."

[dependencies]
cc-ipc-protocol = { workspace = true }
cc-types = { workspace = true }
serde_json = { workspace = true }
