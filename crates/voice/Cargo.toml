[package]
name = "cc-voice"
version = "0.1.0"
edition = "2021"
description = "Voice dictation compatibility state machine and backend contracts."

[lib]
path = "src/mod.rs"

[dependencies]
async-trait = { workspace = true }
cc-auth = { workspace = true }
parking_lot = { workspace = true }
tokio = { workspace = true }

[dev-dependencies]
serial_test = { workspace = true }
