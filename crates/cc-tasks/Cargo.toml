[package]
name = "cc-tasks"
version = "0.1.0"
edition = "2021"
description = "Task domain types, parsing, errors, and persistence shapes."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tokio-util = { workspace = true }
