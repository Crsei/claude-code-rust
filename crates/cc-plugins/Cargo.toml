[package]
name = "cc-plugins"
version = "0.1.0"
edition = "2021"
description = "Plugin loader (discovery, manifest parsing, tool contribution). Phase 7 workspace split (scaffold)."

[dependencies]
anyhow = { workspace = true }
cc-types = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
