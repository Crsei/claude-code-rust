[package]
name = "cc-tools"
version = "0.1.0"
edition = "2021"
description = "Shared tool specs and pure registry helpers."

[dependencies]
cc-tasks = { workspace = true }
serde_json = { workspace = true }
