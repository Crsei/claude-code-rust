//! Root-owned skill runtime adapters.
//!
//! The reusable skill registry lives in `cc-skills`; this module keeps the
//! conversation-facing Skill tool while it still needs root/engine adapters.

pub mod tool;
