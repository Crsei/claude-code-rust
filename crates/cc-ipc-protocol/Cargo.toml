[package]
name = "cc-ipc-protocol"
version = "0.1.0"
edition = "2021"
description = "Shared IPC protocol DTOs for cc-rust frontend/backend boundaries."

[dependencies]
cc-types = { path = "../cc-types", default-features = false }
serde = { workspace = true }
serde_json = { workspace = true }
