//! cc-daemon — KAIROS daemon contracts and runtime owner scaffold.
//!
//! Phase 9 keeps process entry and root runtime adapters in the binary while
//! this crate owns reusable daemon contracts that do not depend on root modules.

pub mod protocol;
pub mod web;
