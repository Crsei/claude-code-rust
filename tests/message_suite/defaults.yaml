# Shared defaults merged into every case's `prereq` (case overrides win).
# Keep secrets here so individual yaml files stay reviewable.
prereq:
  env:
    # DeepSeek is cheap + fast enough for live suite runs.
    CLAUDE_MODEL: "deepseek-chat"
    HTTPS_PROXY: "http://127.0.0.1:7890"
    HTTP_PROXY: "http://127.0.0.1:7890"
    RUST_LOG: "warn"
  # Bypass permission prompts so tool-use cases don't hang waiting for UI.
  permission_mode: bypass
